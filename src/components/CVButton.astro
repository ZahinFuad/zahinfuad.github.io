---
import { profile } from '../data/profile';

interface Props {
  label?: string;
}

const { label = 'Download CV' } = Astro.props;
---

<a class="button-link primary" href={profile.cv} target="_blank" rel="noreferrer">{label}</a>
